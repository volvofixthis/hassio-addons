[tool.poetry]
name = "mountit-util"
version = "1.0.1"
description = "Utility for Mount It Addon"
authors = ["Mount It"]
license = "MIT"
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
diskinfo = "^3.1.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
